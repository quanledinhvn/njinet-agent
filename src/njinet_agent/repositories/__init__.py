"""Data access layer.

No repositories yet, because there are no models yet (see
`njinet_agent.models`). The agent reads and writes data through MCP tools
pointing at the NestJS backend, never touching the database directly.

Put repositories here once this service has tables of its own to query.
"""
